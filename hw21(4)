####Написати sql запит, який буде робити те саме, що і в завданні 3, але виводити дані відсортовані 
### по кількості користувачів у спадаючому порядку та по віку у зростаючому порядку.
SELECT age, count(id) as users
FROM users
GROUP BY age 
ORDER BY users DESC, age ASC